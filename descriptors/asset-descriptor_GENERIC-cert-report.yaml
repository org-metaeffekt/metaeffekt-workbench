inventories:
  - "inventory":
      file: "${inventory.path}"
      type: INPUT
  - "filtered-inventory":
      file: "${param.computed.inventory.dir}/${asset.name}-vulnerability-report-inventory.xlsx"
      type: COMPUTED
  - "filtered-enriched-inventory":
      file: "${param.computed.inventory.dir}/${asset.name}-statistics-report-inventory.xlsx"
      type: COMPUTED

transformations:
  "transform-with-maven":
    inputInventories:
      - "inventory"
    outputInventories:
      - "filtered-inventory"
      - "filtered-enriched-inventory"
    params:
      input.inventory.file: "${inventory.file}"
      param.vulnerabilities.advisory.filter: '[{"name":"${overview.advisors}"}]'
      param.filtered.inventory.file: "${filtered-inventory.file}"
      output.enrichment.inventory: "${filtered-enriched-inventory.file}"
      env.vulnerability.mirror.dir: "${env.vulnerability.mirror.dir}"
      param.security.policy.file: "${security.policy.file}"
      param.security.policy.active.ids: "cert_extended_configuration"
    maven:
      pom: "${workbench.processors.dir}/util/util_merge-filter-enrich-advisors.xml"
      workingDir: "."
      command: "generate-sources"

documents:
  "cert-report":
    type: VULNERABILITY_REPORT
    language: "${document.language}"
    params:
      #parameters for generated parts
      securityPolicyFile: "${security.policy.file}"

      #parameters for generated properties
      vulnerability.report.properties.reference: "filtered-inventory"
      vulnerability.summary.properties.reference: "filtered-enriched-inventory"

      #parameters for report content references (match with inventoryRef)
      vulnerability.summary.part.content.reference: "filtered-enriched-inventory"
      vulnerability.report.content.reference: "filtered-inventory"
      vulnerability.statistics.report.content.reference: "filtered-enriched-inventory"
    parts:
      "summary-report":
        type: VULNERABILITY_SUMMARY_PART
        params:
          securityPolicyActiveIds: "cert_extended_configuration"
        inventories:
          - inventoryRef: "filtered-enriched-inventory"
            assetName: "${asset.name}"
            assetVersion: "${asset.version}"

      "vulnerability-report":
        type: VULNERABILITY_REPORT
        params:
          securityPolicyActiveIds: "cert_extended_configuration"
        inventories:
          - inventoryRef: "filtered-inventory"
            assetName: "${asset.name}"
            assetVersion: "${asset.version}"

      "statistics-report":
        type: VULNERABILITY_STATISTICS_REPORT
        params:
          securityPolicyActiveIds: "cert_configuration"
        inventories:
          - inventoryRef: "filtered-enriched-inventory"
            assetName: "${asset.name}"
            assetVersion: "${asset.version}"
